package testing;

import pieces.*;
import board.Board;
import board.Move;
import board.Board.Builder;

import java.util.ArrayList;
import java.util.Collection;
import java.util.Iterator;
public class MeneliosTesting {

	public static void main(String[] args) {
		//array of test pieces 
		Piece[] ar = new Piece[1] ;
		
		//test Piece
		ar[0] = new Knight(0, Team.BLACK);
		
		//adding test pieces to array
	
		
		//Board for testing
		Board b = Board.createTestBoard(ar);
		
		//testing
		System.out.println(" Knights at "+b.getTile(0).getPiece().getPiecePosition());
		
		Move[] moves = 	ar[0].calculateLegalMoves(b).toArray(new Move[0]);
		

		
		
		System.out.println("--------------------------"+b.getTile(0).getPiece().getPieceType());

		if(b.getTile(0).getPiece().getPieceType() == Piece.PieceType.KNIGHT) {
			System.out.println("&&&&&&&&&&&&&&&&&&&&&&");
		}
	}

}
